sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t yacaschdkr/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true yacaschdkr/react-test npm test

after_success:
  - docker build -t yacaschdkr/multi-client ./client
  - docker build -t yacaschdkr/multi-nginx ./nginx
  - docker build -t yacaschdkr/multi-server ./server
  - docker build -t yacaschdkr/multi-worker ./worker
  # Log in to the docker CLI - to push image to docker hub
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push yacaschdkr/multi-client
  - docker push yacaschdkr/multi-nginx
  - docker push yacaschdkr/multi-server
  - docker push yacaschdkr/multi-worker
